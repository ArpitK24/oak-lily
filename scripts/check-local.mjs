import {chromium} from 'playwright';import fs from 'node:fs/promises';
const b=await chromium.launch({headless:true,args:['--no-sandbox']});const p=await b.newPage({viewport:{width:1440,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(String(e)));await p.route(/^https?:\/\/(?!localhost)/,r=>r.abort());await p.goto('http://localhost:5173/',{waitUntil:'domcontentloaded',timeout:60000});await p.waitForTimeout(2000);console.log('loaded',await p.title());await p.evaluate(()=>{document.querySelectorAll('video').forEach(v=>v.pause());document.querySelectorAll('.scroll-trigger').forEach(v=>v.classList.remove('scroll-trigger--offscreen'));});await p.screenshot({path:'qa/local-desktop.png',fullPage:true,timeout:60000});console.log('shot');await fs.writeFile('qa/local-errors.json',JSON.stringify(errors));console.log(errors.slice(0,10));await p.setViewportSize({width:390,height:844});await p.reload({waitUntil:'domcontentloaded'});await p.waitForTimeout(1000);await p.screenshot({path:'qa/local-mobile.png',fullPage:true,timeout:60000});await b.close();
